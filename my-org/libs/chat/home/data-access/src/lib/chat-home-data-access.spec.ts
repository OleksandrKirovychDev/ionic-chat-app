import { chatHomeDataAccess } from './chat-home-data-access';

describe('chatHomeDataAccess', () => {
  it('should work', () => {
    expect(chatHomeDataAccess()).toEqual('chat-home-data-access');
  });
});
