import { chatSharedInterfaces } from './chat-shared-interfaces';

describe('chatSharedInterfaces', () => {
  it('should work', () => {
    expect(chatSharedInterfaces()).toEqual('chat-shared-interfaces');
  });
});
