import { chatSharedDataAccess } from './chat-shared-data-access';

describe('chatSharedDataAccess', () => {
  it('should work', () => {
    expect(chatSharedDataAccess()).toEqual('chat-shared-data-access');
  });
});
